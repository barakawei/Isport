!!!
%html
  %head
    %title
      isport
    = include_stylesheets :default, :media => 'all'
    = include_stylesheets :blueprint, :media => 'screen'
    = include_javascripts :jquery
    = include_javascripts :main
    = include_javascripts :jqueryui
    = include_javascripts :jquery_form

    = csrf_meta_tag
    = yield :head
     
  %body{:class => "#{yield(:body_class)}"}
    .header{:class => ( 'landing' unless current_user)}
      = render 'layouts/header' 
    .accout.overlay.hide
      .accout_body.overlay_body
        %ul
          %li
            = link_to t('settings'), edit_user_path
          %li
            = link_to t('logout'), destroy_user_session_path
    .container.body
      .span-24.last
        =yield 
      .span-24
        %br
        %br
        %br
    .footer
      .container

:javascript
  $(function() {
    $.datepicker.setDefaults($.datepicker.regional["zh-CN"]); 

    $('.button').button();  
    if (!$.isEmptyObject($('.setting')[0])) {  
      var left = $('.setting li').offset().left;
      var width = $('.setting li').width();
      $('.accout').css({"left":left+"px","width":width});

      $('.current_user_link').click(function(e) {
        $('.accout').toggleClass('hide');
        e.stopPropagation();
        return false
      });

      $('html').click(function(e) {
        $('.accout').addClass('hide');
        $('#city_select_box').addClass('hide'); 
      });
    }
    
    $.getScript("http://v2.jiathis.com/code/jia.js");
  });



