-content_for :head do
  = include_stylesheets :sepcial_pages_groups_common, :media => 'all'
  = include_stylesheets :forum_large, :media => 'all'
.span-24
  .span-24
    .span-10
      = render 'common/header_box', :title => '"' + @group.name + '"' + '的讨论区'
    %h4.inline= link_to '<返回小组首页', group_path(@group), :class => "right link_red"
    = link_to(new_group_topic_path(@group)) do
      .new_button.glass_button
        ='发起话题'
  .span-24.topics_area
    = render "topics/index"

