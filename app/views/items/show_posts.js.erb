content = "<%= escape_javascript(render('status_messages/status_messages_container')) %>"; 
page = '<%=@page%>';
item_id = "<%=@item.id%>";
if (page == ''){ 
  $('.stream').html(content);
}else{
  $('.stream').append(content);
}
tab = '<%=@tab%>';
if(tab != ''  ){
  $('.current').removeClass('current');
  $('#'+tab).addClass('current');
}
$(".tab_progress").hide();
$("#item_posts").attr( "href","/items/"+item_id+"/show_posts" );




