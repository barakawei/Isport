- content_for :head do
  =include_javascripts :show_photo
  =include_javascripts :status_message
  = include_stylesheets :sepcial_pages_home_index, :media => 'all'
.span-24.last
  .span-20.append-2.prepend-2.last
    .content_panel.stream.status_message_show
      =render 'status_messages/status_message_element',:post => @post

:javascript
  $("abbr.timeago").timeago();
