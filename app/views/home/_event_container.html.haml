= render :partial => 'events/home_event_element',
         :collection => @events,
         :as => :event
= will_paginate @events,:page_links => false,:next_label=> '',:previous_label=>''

:javascript
  $( ".map_link" ).colorbox({ transition:"none",opacity:"0.3",iframe:true,width:'750px',height:'400px'});
